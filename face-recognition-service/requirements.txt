flask>=3.0.0
flask-cors>=4.0.0
numpy>=1.26.0
Pillow>=10.0.0
python-dotenv>=1.0.0
gunicorn>=21.0.0

# Optional: For full face recognition (requires cmake and dlib)
# To install: brew install cmake && pip install dlib face_recognition
# face_recognition>=1.3.0
